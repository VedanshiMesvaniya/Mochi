"""
Calendar integration (spec section 22).

Currently just Mode B (Google Calendar, read-only - V3). Mode A (a fully
local calendar table, no Google account required) is a separate, smaller
future addition and doesn't live here.
"""
