# Core - required to run Mochi
PySide6>=6.6.0
python-dotenv>=1.0.0

# Nothing else is required. Open-ended chat gets richer answers if Ollama
# (https://ollama.com) is installed and running locally - Mochi talks to
# it over plain HTTP (app/ai/llm.py), so no Python client package is
# needed. Everything else (reminders, tasks, timers, rule-based chat,
# SQLite storage) works with just the two packages above.
